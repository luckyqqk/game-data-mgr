var Hash = module.exports;

Hash.getCacheOrder = function(key) {
};

Hash.makeKey = function(tableName, foreign, priValue, forValue) {
};

Hash.addCache = function(redis, name, primary, foreign, data, expire, cb) {
};

Hash.remCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};

Hash.updCache = function(redis, name, primary, foreign, json, expire, cb) {
};

Hash.getCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};
