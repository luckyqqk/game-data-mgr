var Zset = module.exports;

Zset.getCacheOrder = function(key) {
};

Zset.makeKey = function(tableName, foreign, priValue, forValue) {
};

Zset.addCache = function(redis, name, primary, foreign, data, expire, cb) {
};

Zset.remCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};

Zset.updCache = function(redis, name, primary, foreign, json, expire, cb) {
};

Zset.getCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};
