var Set = module.exports;

Set.getCacheOrder = function(key) {
};

Set.makeKey = function(tableName, foreign, priValue, forValue) {
};

Set.addCache = function(redis, name, primary, foreign, data, expire, cb) {
};

Set.remCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};

Set.updCache = function(redis, name, primary, foreign, json, expire, cb) {
};

Set.getCache = function(redis, name, primary, foreign, priValue, forValue, cb) {
};
